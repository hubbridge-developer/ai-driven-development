# Backend API (Django + Channels served by Daphne).
# Service is named "aidd-api" to match the frontend vite proxy and docker-compose.
apiVersion: cloud.google.com/v1
kind: BackendConfig
metadata:
  name: aidd-api-backendconfig
  namespace: aidd
spec:
  # Keep long-lived WebSocket connections (ws/workflow/{id}) from being cut by
  # the GCE L7 load balancer.
  timeoutSec: 3600
  connectionDraining:
    drainingTimeoutSec: 60
---
apiVersion: v1
kind: Service
metadata:
  name: aidd-api
  namespace: aidd
  labels: { app: aidd-api }
  annotations:
    cloud.google.com/backend-config: '{"default": "aidd-api-backendconfig"}'
spec:
  selector: { app: aidd-api }
  ports:
    - port: 8001
      targetPort: 8001
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: aidd-api
  namespace: aidd
spec:
  replicas: 1                        # scale >1 only after moving seeds to a Job
  selector:
    matchLabels: { app: aidd-api }
  template:
    metadata:
      labels: { app: aidd-api }
    spec:
      # initContainer runs DB migrations + one-time seeds before Daphne starts.
      initContainers:
        - name: migrate-and-seed
          image: aidd-backend:latest      # overridden by kustomization images:
          command:
            - sh
            - -c
            - >
              python manage.py migrate --noinput &&
              python manage.py qdrant_create_collection &&
              python manage.py seed_namespaces &&
              python manage.py seed_spec_repo &&
              python manage.py seed_repositories
          envFrom:
            - configMapRef: { name: aidd-config }
            - secretRef: { name: aidd-secrets }
      containers:
        - name: api
          image: aidd-backend:latest        # overridden by kustomization images:
          command: ["daphne", "-b", "0.0.0.0", "-p", "8001", "aidd_project.asgi:application"]
          ports:
            - containerPort: 8001
          envFrom:
            - configMapRef: { name: aidd-config }
            - secretRef: { name: aidd-secrets }
          readinessProbe:
            httpGet:
              path: /admin/login/     # returns 200 — used as the GCE backend health check
              port: 8001
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 8001
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            requests: { cpu: "250m", memory: "512Mi" }
            limits: { cpu: "1", memory: "2Gi" }   # embedding model needs headroom
