apiVersion: v1
kind: Service
metadata:
  name: add-postgres
  namespace: add
  labels: { app: add-postgres }
spec:
  selector: { app: add-postgres }
  ports:
    - port: 5432
      targetPort: 5432
  clusterIP: None          # headless, for the StatefulSet
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: add-postgres
  namespace: add
spec:
  serviceName: add-postgres
  replicas: 1
  selector:
    matchLabels: { app: add-postgres }
  template:
    metadata:
      labels: { app: add-postgres }
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              value: "add"
            - name: POSTGRES_DB
              value: "add"
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: add-secrets
                  key: POSTGRES_PASSWORD
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          readinessProbe:
            exec:
              command: ["sh", "-c", "pg_isready -U add"]
            initialDelaySeconds: 10
            periodSeconds: 5
          resources:
            requests: { cpu: "100m", memory: "256Mi" }
            limits: { cpu: "500m", memory: "512Mi" }
          volumeMounts:
            - name: pgdata
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: pgdata
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 5Gi
